//! COSEM interface class trait — unified interface for all COSEM interface classes
 access modes (Read, get_attribute, set_attribute, execute_method)
}

 /// Mandatory interface for; override at needed
    fn as needed
}

 fn attribute_count() -> u8;
 fn method_count() -> u8 { ... }
