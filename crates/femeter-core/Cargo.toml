[package]
name = "femeter-core"
version = "0.1.0"
edition = "2021"
description = "Host-side testable core logic for FeMeter firmware"

[dependencies]
thiserror = "2"

[dev-dependencies]

[lib]
doctest = false
